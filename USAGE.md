# PanelTextor 使い方

[日本語](USAGE.md) | [English](USAGE.en.md) | [简体中文](USAGE.zh-CN.md) | [한국어](USAGE.ko.md)

Windows 11用のローカル多言語テキスト配置ツールです。

## 起動

配布ZIPを展開して `PanelTextor.exe` をダブルクリックしてください。初回は利用条件・第三者原文を確認し、同意して起動します。exe単体でも動作し、.NETの事前インストールは不要です（Windows 11 x64）。必要な実行ファイルは起動時に一時領域へ展開されます。

## 使い方

画面上部の「Language」で、日本語／English／简体中文／한국어を選択できます。ボタン・説明・設定・書き出し画面の表示が切り替わり、次回起動時も選択を保持します。初期値は日本語です。本文を編集するJP／EN／CN／KRとは独立しており、入力済みの文章やレイアウトは変更しません。Windows標準ダイアログのシステムボタンやOS由来のエラーはWindowsの表示言語に従います。

1. 「画像を追加」でJPEG / PNGなどを複数選択します。
2. 左のページ一覧と上のJP / EN / CN / KRを選びます。編集中の言語ボタンは青色になります。ページを移動するとJPへ戻ります。
3. 右の入力欄に文章を貼り付け、「空行で分割して追加」を押します。空行が区切り、通常の改行は同じブロック内に残ります。
4. キャンバス上でドラッグして配置します。右側の一覧からも選択できます。
   Ctrl＋クリックで複数選択できます（画像上・一覧の両方）。一覧ではShift＋クリックで範囲選択もできます。「選択テキストを削除」ボタン、または画像上／一覧にフォーカスがある状態でDeleteキーを押すと、選択したテキストをまとめて削除します。文字入力中のDeleteは通常の文字削除です。複数選択中は件数を表示し、文章・サイズなどの個別編集は単独選択時に行います。
5. 小・中・大、4色、JPの縦・横をワンクリックで変更できます。個別の文章は選択中テキスト欄で編集します。
   右側の「行間（行送り px）」で選択中テキストの間隔を直接変更できます。横書きは行頭から次の行頭まで、縦書きは隣の列までの距離です。「既定」または0で言語の設定へ戻ります。改行のない1行・1列には変化がありません。空行で分割された別ブロック同士の距離はドラッグで調整します。
6. 「プリセット設定」でOSフォント、言語別サイズ・縁取り、共通色を設定します。既存の全テキストにも反映されます。可変フォントは直下の「ウェイト」で太さを選択できます。Source Han Sans CN VFではRegular (400)／Medium (500)／Bold (700)などに対応し、フォント本来のwght軸を横書き・縦書き・書き出しに反映します。「既定」は従来の表示を維持します。設定は言語ごと・全プロジェクト共通です。
7. 「保存」でJSONプロジェクトを保存。「開く」で再開できます。
8. 現在ページまたは全ページを書き出します。書き出す言語（JP／EN／CN／KR、複数選択可）、画像セットとPNG／JPEGを選択すると、「出力先／言語／セット名／元の名前.png（または.jpg）」へまとめて保存します。同名があれば連番を付けます。

## 撮影フレーム

右側の「撮影フレームを表示」をONにすると、四隅・REC・経過時間・バッテリー・中央フォーカスを表示します。Batteryの1〜5をクリックすると即座に反映します。1は外枠・端子・残量バーが赤、2〜5は白です。経過時間はmm:ssまたはhh:mm:ssで入力します（自動カウントはしません）。

ON/OFF・残量・経過時間はページごとにプロジェクトへ保存され、同じページの全言語・全背景セットで共有します。PNG／JPEGにも合成されます。旧プロジェクトでは初期状態がOFFなので表示は変わりません。各要素は独立した設定とベクター描画を持ち、固定PNGは使用しません。REC点滅や4K・FPS・ISO等の追加設定UIは今回の対象外です。

## 複数の背景画像セットの操作

1. 「画像セットを追加／更新」で最初のフォルダー（例：Mosaic）を選びます。最初のセットの画像がページ一覧になります。従来の「画像を追加」も利用できます。
2. 同じボタンでBlackMosaicなど別のフォルダーを追加します。セット数の上限は設けていません。
3. 拡張子を含む同じファイル名を対応付けます（大文字小文字は区別しません）。テキストはページ＋言語に一度だけ保持します。
4. 「Background」で背景を切り替えます。文章・位置・サイズ・色・方向は共有され、背景変更では変わりません。
5. 書き出し時に必要なセットすべてへチェックを入れます。選択はプロジェクトに保存されます。

不足・余分なファイル・解像度の違いは追加時と書き出し前に警告します。「対応チェック」でも確認できます。解像度の違う画像は勝手に拡大縮小せず、出力対象から除外します。不一致がある場合のみ、正常な画像だけ出力するか確認します。追加セット側だけの余分な画像は自動的にページへ追加しません。

フォルダーの画像を修正したら、同じフォルダーを「画像セットを追加／更新」で選び直してください。サブフォルダーは再帰的には読み込みません。旧データに同名ページが複数ある場合、旧レイアウトは残し、追加セットの対応付けを停止します。

## 保存と画像

- 元画像は変更しません。プロジェクトには元画像への相対パスを保存します。プロジェクトと背景フォルダーの相対位置を保って移動してください。旧形式の元画像が見つからなければ開く際に再指定できます。
- プロジェクト形式はVersion 2です。Version 1は読み込み時にメモリ内で自動移行し、元ファイルはその時点では変更しません。Version 1ファイルへの初回上書き保存時に、元の内容を .v1.bak として残します。既存バックアップがあれば連番になります。Version 2は旧アプリでは開けません。
- アプリ設定は `%LOCALAPPDATA%/PanelTextor/settings.json` に保存します。プリセットは全プロジェクト共通なので、変更すると以前の作品の描画にも反映されます。
- 言語の表示スイッチはページ・言語ごとの編集プレビュー用です。書き出しでは対象言語の全テキストを描画します。
- プレビューは長辺1600pxまで、直近6画像をキャッシュ。書き出しは1ページずつ元解像度で処理します。

## 現時点の制限

- 日本語縦書きはHarfBuzzによるOpenType縦書き字形とUnicodeの方向分類に対応しています。連続する半角 ! / ? と2〜3桁の半角数字は自動で縦中横になります。ルビ・禁則処理・自動改行は未対応です。可変フォントの軸は太さ（wght）の選択に対応し、幅・光学サイズ等は未対応です。
- EN / CN / KRはUI上は横書きのみ。各言語の適切なフォントをWindowsにインストールしてください。
- 背景の埋め込みICCをsRGBへ変換して合成し、PNG／JPEGへsRGB ICCを埋め込みます。元のPPIを保持します。ICCなしのRGB画像はsRGBと仮定します。元のICCをそのまま付け直す方式ではありません。
- EXIF回転、HDR、CMYKの制作・印刷用色管理、元メタデータ全体の保存は対象外です。画像は画素の向きを基準に読み込みます。JPEGは品質100で、透明部分は白になります。
- 文字サイズはpxのまま維持しています。画面のモニタープロファイル補正や他の画像編集ソフト固有のAAを完全再現するものではありません。
- 自動折り返しはありません。改行で調整してください。画像外にはみ出した文字は書き出し時に切れます。
- 元画像を後で別の画像に置き換えないでください。数億画素など極端に大きな画像の出力はメモリ容量に依存します。
- Undo / Redo、吹き出し・擬音は今回のMVP対象外です。

書き出し中は画面下部に進捗ゲージ・処理件数・割合を表示します。終了時には成功件数と警告を表示するポップアップが開き、OKで閉じます。中国語・韓国語の出力フォルダー名はCN・KRです。既存データとの互換性のため、プロジェクト内部の言語キーは従来のまま保持します。


保存形式のファイル名は `作品.paneltextor.json` です。旧.polytext.jsonも開けます。旧設定は初回起動時にPolyTextフォルダーからPanelTextorフォルダーへコピーされ、元ファイルは残ります。第三者ライセンスはアプリ上部の「ライセンス」から閲覧できます。







## 取り込み時の置換ルール

「空行で分割して追加」の下にある「取り込み時の置換ルール」を開きます。JP／EN／CN／KRのタブを選び、表の最下行に「置換前」「置換後」を入力して保存してください。有効のチェックを外すとルールを停止でき、「選択ルールを削除」で削除できます。キャンセルした変更は保存しません。

ルールは言語ごとに設定ファイルへ保存し、全プロジェクトで共通です。新規追加時、空行で分割する前に上から順に適用します。大文字小文字を区別する部分一致の文字列置換で、正規表現ではありません。置換した結果も次のルールの対象です。置換後が空欄なら一致部分を削除します。空の置換前は登録できません。例：JPに「貴方 → あなた」を登録すると、新しく取り込むJPの文章だけが置換されます。既存テキストや個別編集には適用しません。
