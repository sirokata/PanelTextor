# PanelTextor 사용 방법

[日本語](USAGE.md) | [English](USAGE.en.md) | [简体中文](USAGE.zh-CN.md) | [한국어](USAGE.ko.md)

## 실행 및 화면 언어

배포ZIP을 풀고 `PanelTextor.exe`를 실행하세요. 최초 실행 시 약관 및 제3자 원문을 읽고 동의합니다. Windows 11 x64가 필요하며 .NET은 별도 설치하지 않아도 됩니다. 런타임 파일은 임시 폴더에 풀립니다.

상단 **Language**에서 일본어·영어·간체 중국어·한국어를 선택합니다. 선택은 저장되며 기본값은 일본어입니다. 화면만 바뀌고 JP/EN/CN/KR 텍스트 레이어나 기존 배치는 바뀌지 않습니다. Windows 시스템 버튼과 OS 오류는 Windows 표시 언어를 따릅니다.

## 텍스트 추가 및 편집

1. ‘이미지 추가’로 JPEG, PNG 등 지원 이미지를 선택합니다.
2. 페이지와 텍스트 언어 **JP / EN / CN / KR**를 선택합니다. 활성 언어는 파란색입니다. 페이지를 바꾸면 텍스트 언어는 JP로 돌아갑니다.
3. ‘텍스트 일괄 입력’에 붙여 넣고 ‘빈 줄로 분할하여 추가’를 클릭합니다. 빈 줄은 블록 구분, 일반 줄바꿈은 같은 블록 안에 유지됩니다.
4. 캔버스에서 드래그하거나 ‘배치된 텍스트’ 목록에서 선택합니다. 캔버스·목록 모두 Ctrl+클릭으로 다중 선택, 목록은 Shift+클릭으로 범위를 선택합니다. 캔버스·목록에 포커스가 있으면 삭제 버튼 또는 Delete로 선택 블록을 삭제합니다. 입력란의 Delete는 문자를 삭제합니다. 텍스트·서식 편집은 하나만 선택하여 수행합니다.
5. 작게/중간/크게, 4가지 색상, JP의 세로/가로쓰기를 선택합니다. ‘줄/열 간격(px)’은 가로쓰기의 줄 시작점 사이 거리 또는 세로쓰기의 열 간격입니다. 기본값/0은 언어 프리셋을 사용하며 한 줄·한 열에는 영향이 없습니다. 별도 블록 사이 간격은 드래그로 조정합니다.
6. ‘프리셋 설정’에서 글꼴·크기·테두리·공통 색상을 설정합니다. 기존 텍스트와 모든 프로젝트에 적용됩니다. 가변 글꼴은 굵기 선택을 제공하며 Source Han Sans CN VF는 Regular(400), Medium(500), Bold(700) 등을 지원합니다. 실제 wght 축이 가로·세로·내보내기에 적용됩니다. 기본값은 이전 표시를 유지합니다.
7. ‘저장’으로 `*.paneltextor.json`을 저장하고 ‘열기’로 다시 편집합니다.
8. 현재 페이지 또는 모든 페이지를 내보냅니다. 여러 텍스트 언어, 배경 세트, PNG/JPEG를 선택하면 `출력/언어/세트/원래이름.png` (또는.jpg)로 저장합니다. 동일한 이름에는 번호를 붙입니다. 중국어·한국어 폴더는 CN/KR이며 내부 이전 키는 호환성을 위해 유지됩니다.

하단 진행률에는 완료 수, 전체 수, 퍼센트가 표시됩니다. 완료 창에서 성공 수와 경고를 확인하고 OK로 닫습니다.

## 촬영 프레임

‘촬영 프레임 표시’를 켜면 네 모서리·REC·경과 시간·배터리·중앙 초점 표시가 나타납니다. Battery 1–5를 클릭하면 즉시 반영됩니다. 1은 외곽·단자·남은 막대가 빨간색, 2–5는 흰색입니다. 시간은 `mm:ss` 또는 `hh:mm:ss`로 입력하며 자동으로 흐르지 않습니다.

표시 여부·잔량·시간은 페이지별로 저장되고 같은 페이지의 모든 언어·배경에서 공유합니다. PNG/JPEG에도 포함됩니다. 이전 프로젝트의 기본값은 꺼짐입니다. 각 요소는 독립 설정과 벡터로 그리며 고정PNG가 아닙니다. REC 깜박임 및 4K/FPS/ISO 등의 UI는 미지원입니다.

## 여러 배경 세트

1. ‘이미지 세트 추가/업데이트’로 첫 폴더(예: Mosaic)를 선택하면 이미지가 페이지 목록이 됩니다. ‘이미지 추가’도 사용할 수 있습니다.
2. 같은 방식으로 BlackMosaic 등 다른 폴더를 추가합니다. 세트 수에 고정 상한은 없습니다.
3. 확장자를 포함한 동일 파일명으로 대응하며 대소문자는 구분하지 않습니다. 텍스트·배치는 페이지＋언어별 한 번만 저장됩니다.
4. ‘배경’을 바꾸어 미리 봅니다. 텍스트·위치·크기·색상·방향은 공유됩니다.
5. 내보낼 세트를 모두 체크합니다. 세트 선택은 프로젝트에 저장됩니다.

추가 시 및 내보내기 전에 누락·추가 파일·해상도 차이를 알리며 ‘파일 대응 확인’으로도 검사합니다. 해상도가 다르면 자동 조정하지 않고 제외합니다. 불일치가 있을 때 정상 이미지만 내보낼지 묻습니다. 추가 세트에만 있는 파일은 페이지로 자동 추가하지 않습니다.

폴더 내용을 수정한 뒤에는 같은 폴더를 다시 선택해 불러오세요. 하위 폴더는 재귀 검색하지 않습니다. 이전 데이터에 같은 이름의 페이지가 여러 개면 기존 배치를 유지하고 추가 세트 대응을 중단합니다.

## 파일·설정·이미지

- 원본은 변경하지 않습니다. 프로젝트에 상대 경로를 저장하므로 프로젝트와 배경 폴더의 상대 위치를 유지해서 이동하세요. 기존 프로젝트를 열 때 누락 원본의 위치를 다시 지정할 수 있습니다.
- 프로젝트 형식은 Version 2입니다. Version 1은 열 때 메모리에서 변환하며 파일은 즉시 바꾸지 않습니다. Version 1 파일을 덮어쓸 때 `.v1.bak`으로 백업하고 중복이면 번호를 붙입니다. Version 1 전용 앱은 Version 2를 열 수 없습니다.
- 기존 `.polytext.json`도 열 수 있으며 저장 시 `.paneltextor.json`을 제안합니다. 이전 파일은 자동 삭제하지 않습니다.
- 설정은 `%LOCALAPPDATA%/PanelTextor/settings.json`입니다. 없으면 `%LOCALAPPDATA%/PolyText/settings.json`을 복사하고 원본을 유지합니다. 프리셋은 모든 프로젝트에 공통이므로 바꾸면 과거 작품 표시도 달라집니다.
- 동의 기록은 Windows 사용자별 `%LOCALAPPDATA%/PanelTextor/terms-acceptance.txt`에 저장됩니다. 약관 변경 시 재동의하며 ‘라이선스’에서 원문을 볼 수 있습니다.
- 언어 표시 스위치는 페이지/언어 미리 보기에만 적용됩니다. 내보내기는 선택 언어의 모든 텍스트를 포함합니다.
- 미리 보기는 긴 변 최대1600픽셀, 최근6개 이미지를 캐시합니다. 내보내기는 원본 해상도로 한 장씩 처리합니다.

## 렌더링·제한

일본어 세로쓰기는 HarfBuzz/OpenType 세로 글리프와 Unicode 방향 정보를 사용합니다. 연속 반각 `!`/`?`, 2–3자리 숫자는 세로 흐름 안에서 가로로 조합됩니다. 루비·금칙·자동 줄바꿈은 미지원입니다. 가변 축은 wght만 지원하며 폭·광학 크기는 미지원입니다. EN/CN/KR는 UI상 가로쓰기만 됩니다. 적절한 글꼴을 설치하세요. 

배경ICC를 sRGB로 변환하고 출력에 sRGB ICC를 포함하며 원래 PPI를 유지합니다. ICC가 없는 RGB는 sRGB로 간주하며 불일치하는 원래 프로파일을 다시 붙이지 않습니다. EXIF 회전, HDR, CMYK 인쇄 처리, 전체 메타데이터 유지는 대상이 아닙니다. 픽셀 방향을 기준으로 합니다. JPEG 품질100의 투명 부분은 흰색이며 여전히 손실 압축입니다.

크기는 이미지 픽셀입니다. 모니터 프로파일 보정 및 다른 이미지 편집기 고유 앤티앨리어싱과 완전히 일치하지 않습니다.  수동 줄바꿈이 필요하며 이미지 밖 텍스트는 잘립니다. 원본을 나중에 임의로 교체하지 마세요. 매우 큰 이미지는 메모리 용량에 영향을 받습니다. 실행 취소/다시 실행·말풍선·효과음 디자인은 미지원입니다.



## 가져오기 치환 규칙

‘빈 줄로 분할하여 추가’ 아래의 ‘가져오기 치환 규칙’을 엽니다. JP/EN/CN/KR 탭을 선택하고 표의 마지막 행에 찾을 문자열과 바꿀 문자열을 입력한 뒤 저장하세요. 사용 체크를 해제하면 규칙을 중지하고 ‘선택 규칙 삭제’로 삭제합니다. 취소하면 변경을 저장하지 않습니다.

규칙은 텍스트 언어별로 앱 설정에 저장되며 모든 프로젝트에 공통입니다. 새 텍스트를 추가할 때 빈 줄 분할 전에 위에서 아래로 적용합니다. 대소문자를 구분하는 일반 문자열 부분 일치이며 정규식은 사용하지 않습니다. 치환 결과도 다음 규칙의 대상입니다. 바꿀 문자열이 비어 있으면 일치 부분을 삭제하며 찾을 문자열은 비울 수 없습니다. 예를 들어KR의 ‘당신 → 너’ 규칙은 새로 가져오는KR 텍스트에만 적용됩니다. 기존 블록이나 개별 편집에는 적용하지 않습니다.
