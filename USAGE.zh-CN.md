# PanelTextor 使用说明

[日本語](USAGE.md) | [English](USAGE.en.md) | [简体中文](USAGE.zh-CN.md) | [한국어](USAGE.ko.md)

## 启动与界面语言

解压分发ZIP并运行 `PanelTextor.exe`。首次启动时阅读并同意使用条款及第三方原文。需要Windows 11 x64，无需单独安装.NET。运行所需文件会解压到临时目录。

顶部 **Language** 可选择日语、英语、简体中文、韩语，选择会保存，默认为日语。它只改变界面，不改变JP/EN/CN/KR文字图层或现有排版。Windows系统按钮及系统错误消息使用Windows的显示语言。

## 添加和编辑文字

1. 点击“添加图片”，选择JPEG、PNG等支持的图片。
2. 选择页面和文字语言 **JP / EN / CN / KR**。当前语言按钮为蓝色，切换页面后文字语言回到JP。
3. 在“批量输入文字”中粘贴文章，点击“按空行拆分并添加”。空行分隔文字块，普通换行保留在同一块内。
4. 在画布上拖动文字块，或从“已放置的文字”列表选择。画布和列表均支持Ctrl+单击多选；列表中Shift+单击可选范围。焦点在画布或列表时，用删除按钮或Delete删除选中块。输入框内Delete正常删除字符。单选后可编辑文字及格式。
5. 选择小/中/大、4种颜色之一，以及JP的竖排/横排。“行／列间距(px)”表示横排相邻行起点距离或竖排列间距离。默认/0使用语言预设；单行或单列不受影响。不同文字块之间用拖动调整。
6. “预设设置”配置字体、字号、描边和共用颜色，对现有文字及所有项目生效。可变字体提供字重选择：Source Han Sans CN VF支持Regular(400)、Medium(500)、Bold(700)等。真实wght轴用于横排、竖排和导出；“默认”保留原有外观。
7. “保存”为 `*.paneltextor.json`，“打开”可继续编辑。
8. 导出当前页或所有页面。多选文字语言、图片集及PNG/JPEG，保存至 `输出目录/语言/图片集/原文件名.png`（或.jpg）。同名输出添加序号。中文/韩文目录为CN/KR，内部旧语言键为兼容性而保留。

底部进度条显示完成数、总数及百分比。完成弹窗显示成功导出数量与警告，点击OK关闭。

## 拍摄取景框

开启“显示拍摄取景框”后显示四角、REC、已用时间、电池及中央对焦标记。点击Battery 1–5立即预览。1的外框、端子及剩余电量条为红色；2–5为白色。时间输入格式为 `mm:ss` 或 `hh:mm:ss`，不会自动计时。

开关、电量、时间按页面保存，同一页的所有语言和背景集共享，并合成到PNG/JPEG。旧项目默认关闭。各元素使用独立设置和矢量绘制，而非固定PNG。暂不提供REC闪烁、4K/FPS/ISO等控制。

## 多个背景图片集

1. “添加／更新图片集”选择首个文件夹，如Mosaic，其图片成为页面列表。也可继续使用“添加图片”。
2. 同样添加BlackMosaic等其他文件夹，未硬编码数量上限。
3. 按包含扩展名的同名文件匹配，不区分大小写。文字及排版按页面＋语言只保存一份。
4. 用“背景”切换预览。文字、位置、字号、颜色及方向保持共享。
5. 导出时勾选所有需要的图片集，图片集选择会保存到项目。

导入和导出前会报告缺失、多余文件及分辨率差异，也可点击“检查文件匹配”。分辨率不同的图片不会自动缩放，而是排除。发生不匹配时会询问是否只导出有效图片。附加图片集中多出的文件不会自动创建页面。

修改文件夹内容后，请重新选择同一文件夹以加载。不递归扫描子文件夹。旧数据中页面名称重复时，无法匹配附加图片集，但保留原排版。

## 文件、设置与图片处理

- 不修改原图。项目保存相对路径，移动项目和背景文件夹时保持相对位置。打开旧项目时可重新指定缺失原图。
- 项目格式为Version 2。打开Version 1时先在内存迁移，不立即修改文件。覆盖Version 1文件时原内容备份为 `.v1.bak`，重名则编号。旧Version 1应用无法打开Version 2。
- 支持打开旧 `.polytext.json`，保存时建议 `.paneltextor.json`，不自动删除旧文件。
- 设置位于 `%LOCALAPPDATA%/PanelTextor/settings.json`。新设置不存在时会自动复制 `%LOCALAPPDATA%/PolyText/settings.json`，保留原文件。预设对所有项目通用，修改也会影响以前作品的渲染。
- 同意记录按Windows用户保存至 `%LOCALAPPDATA%/PanelTextor/terms-acceptance.txt`。条款变化后需重新同意，可通过“许可证”随时查看原文。
- 语言显示开关仅控制页面／语言预览，导出包含所选语言的所有文字。
- 预览长边最大1600像素，缓存最近6张图片。导出逐张按原分辨率处理。

## 渲染与限制

日语竖排使用HarfBuzz/OpenType竖排字形和Unicode方向数据。连续半角 `!`/`?` 及2–3位数字自动纵中横。不支持注音、禁则处理和自动换行。可变字体仅支持wght字重轴，不支持宽度或光学尺寸。界面中EN/CN/KR仅横排，请安装适当字体。

背景ICC转换为sRGB，输出嵌入sRGB ICC并保留原PPI。无ICC的RGB视为sRGB，不会重新附加不匹配的原配置文件。不处理EXIF旋转、HDR、CMYK印刷流程及全部元数据保留，以像素方向为准。JPEG质量100，透明区域为白色，仍属于有损压缩。

字号以图像像素计。不完全重现显示器配置补偿及其他图像编辑器专有抗锯齿。请手动换行，图像范围外文字会被裁剪。不要事后直接替换原图。超大图像受可用内存限制。不支持撤销/重做、对话气泡及拟声词设计。



## 导入替换规则

点击“按空行拆分并添加”下方的“导入替换规则”。选择JP/EN/CN/KR标签，在表格末行输入替换前和替换后并保存。取消启用可停用规则，“删除所选规则”可删除。取消窗口不保存更改。

规则按文字语言保存在应用设置中，所有项目共用。添加新文字时，在按空行拆分前从上到下应用。使用区分大小写的字面子串匹配，不使用正则表达式。后续规则也处理前面的替换结果。替换后为空则删除匹配部分，替换前不能为空。例如CN中设置“您好 → 你好”只影响新导入的CN文字，不改变已有文字块或单独编辑的文字。
